{
  plugins = {
    neo-tree = {
      enable = true;
      lazyLoad = {
        enable = true;
        settings = { cmd = [ "Neotree" ]; };
      };
      window = {
        width = 25;
        mappings = { "l" = "open"; };
      };
    };
  };
}
