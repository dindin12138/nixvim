{
  plugins = {
    neo-tree = {
      enable = true;
      # lazyLoad = {
      #   enable = true;
      #   settings = { cmd = [ "Neotree" ]; };
      # };
      settings = {
        window = {
          width = 25;
          mappings = {
            "l" = "open";
          };
        };
      };
    };
  };
}
